BASE="alpha"
CANDIDATE="1"
REVISION="0"


MAJOR=0
MINOR=1
PATCH=0

CANDIDATE_VERSION=${BASE}-rc.${CANDIDATE}-r${REVISION}
RELEASE_VERSION=${BASE}-${MAJOR}-${MINOR}-${PATCH}
VERSION=${MAJOR}.${MINOR}.${PATCH}-${CANDIDATE_VERSION}